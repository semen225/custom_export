= simple_form_for @export, :html => { :class => 'form-horizontal' } do |f|
  = f.input :person_surname
  = f.input :person_name
  = f.input :person_middle_name
  = f.input :inn
  = f.button :submit
  = f.button :button, 'Cancel', :type => 'reset', :class => 'btn'